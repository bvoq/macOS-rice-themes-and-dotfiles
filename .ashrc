# this is used by iSH on iOS (set up using .profile)
alias pbcopy="cat > /dev/clipboard"
alias pbpaste="cat /dev/clipboard"

totp() { oathtool --totp -b $(<~/".totp_${1}") | pbcopy; }

eval "$(zoxide init posix --hook prompt)"
