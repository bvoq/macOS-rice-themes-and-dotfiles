# Avoid running `gem env gemdir` here; it launches Ruby on every shell startup.
# export PATH="$(gem env gemdir)/bin:$PATH"

# command -v rbenv >/dev/null 2>&1 && ( eval "$(rbenv init - zsh)" || true )
